# @package _global_
# PostgreSQL database configuration (for job tracking)

jobs:
  store:
    postgres_dsn: ${oc.env:CATALYST_JOBS__store__postgres_dsn,postgresql://catalyst:catalyst@postgres:5432/catalystjobs}
    redis_url: ${oc.env:CATALYST_JOBS__store__redis_url,redis://redis:6379/1}
